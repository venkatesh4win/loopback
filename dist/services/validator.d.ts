import { Credentials } from '../repositories';
import { KeyAndPassword } from '../models';
export declare function validateCredentials(credentials: Credentials): void;
export declare function validateKeyPassword(keyAndPassword: KeyAndPassword): void;
